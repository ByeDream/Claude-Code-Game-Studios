/**
 * Run Manager — Configuration Constants
 *
 * @module src/gameplay/run/runConfig
 */

/** Starting honor for a new run. */
export const DEFAULT_STARTING_HONOR = 100

/** Honor lost when losing to a mid-boss. */
export const MID_BOSS_HONOR_COST = 30

/** Honor lost when losing to the final boss. */
export const FINAL_BOSS_HONOR_COST = 100
